<form method="post" action="{{ path('app_test_delete', {'id': test.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test.id) }}">
    <div class="buttons">
        <button class="delete-button">Delete</button>
    </div>
</form>
