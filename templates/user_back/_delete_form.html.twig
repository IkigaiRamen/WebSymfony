<form method="post" action="{{ path('user_back_delete', {'id': user.id}) }}" onsubmit="return confirm('étes vous sûr ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Supprimer</button>
</form>
