{% extends 'back.html.twig' %}

{% block title %}Postule{% endblock %}

{% block body %}
    <h1>Postule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ postule.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ postule.createdAt ? postule.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_postule_back_index') }}">back to list</a>

    <a href="{{ path('app_postule_back_edit', {'id': postule.id}) }}">edit</a>

    {{ include('postule_back/_delete_form.html.twig') }}
{% endblock %}
