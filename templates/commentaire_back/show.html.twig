{% extends 'back.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCommentaire</th>
                <td>{{ commentaire.idCommentaire }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commentaire.date ? commentaire.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_back_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_back_edit', {'idCommentaire': commentaire.idCommentaire}) }}">edit</a>

    {{ include('commentaire_back/_delete_form.html.twig') }}
{% endblock %}
