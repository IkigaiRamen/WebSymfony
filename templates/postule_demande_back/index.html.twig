{% extends 'back.html.twig' %}

{% block title %}PostuleDemande index{% endblock %}

{% block body %}
    <h1>PostuleDemande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created_at</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for postule_demande in postule_demandes %}
            <tr>
                <td>{{ postule_demande.id }}</td>
                <td>{{ postule_demande.createdAt ? postule_demande.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_postule_demande_back_show', {'id': postule_demande.id}) }}">show</a>
                    <a href="{{ path('app_postule_demande_back_edit', {'id': postule_demande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_postule_demande_back_new') }}">Create new</a>
{% endblock %}
