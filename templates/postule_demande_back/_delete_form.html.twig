<form method="post" action="{{ path('app_postule_demande_back_delete', {'id': postule_demande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postule_demande.id) }}">
    <button class="btn">Delete</button>
</form>
